# PDF assets

Drop the real downloadable PDFs here. The site links to these exact filenames:

- `premier-budget-projection.pdf`
- `varsity-budget-projection.pdf`
- `jv-ms-budget-projection.pdf`
- `membership-overview.pdf`

Until a file exists, the corresponding download link 404s gracefully (no crash).
Filenames are referenced in `lib/content.ts` (`BUDGET_PDFS`) and
`components/ContactForm.tsx`.
